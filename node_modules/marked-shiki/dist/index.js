function o(l = {}) {
  const { highlight: a, container: c } = l;
  return {
    async: !0,
    async walkTokens(t) {
      if (!(t.type !== "code" || !t.lang || typeof a != "function"))
        try {
          const [e = "text", ...s] = t.lang.split(" "), { text: n } = t, r = await a(n, e, s), i = c ? c.replace("%l", String(e).toUpperCase()).replace("%s", r).replace("%t", n) : r;
          Object.assign(t, {
            type: "html",
            block: !0,
            text: `${i}
`
          });
        } catch (e) {
          throw e;
        }
    }
  };
}
export {
  o as default
};
